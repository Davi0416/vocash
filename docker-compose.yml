services:
  postgres:
    image: postgres:16-alpine
    container_name: vocash-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: vocash
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: always

volumes:
  pgdata:
